<Window x:Class="ScreenshotXY.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ScreenshotXY" Height="600" Width="980"
        WindowStartupLocation="CenterScreen" SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="8">
            <ComboBox x:Name="ProcessCombo" Width="240" Margin="0,0,8,0"
                      IsEditable="True" IsTextSearchEnabled="True"
                      Text="game.exe">
            </ComboBox>
            <Button x:Name="BtnRefresh"
                    Content="Refresh List"
                    Width="110"
                    Margin="0,0,8,0"
                    Click="BtnRefresh_Click" />
            <Button x:Name="BtnCapture" Content="Capture Screenshot (Ctrl + F12)" Width="130" Click="BtnCapture_Click" />
            <TextBlock x:Name="LblSize"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       FontFamily="Consolas"
                       Text="Screenshot size: — × —" />
        </DockPanel>

        <!-- Main area: left image + right info -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>

            <!-- Left: scrollable image with overlay canvas -->
            <ScrollViewer Grid.Column="0" x:Name="ImageScroll" HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Grid x:Name="ImageHost">
                    <Image x:Name="CapturedImage" Stretch="None"
                           MouseDown="Image_MouseDown" MouseMove="Image_MouseMove" MouseUp="Image_MouseUp" />
                    <Canvas x:Name="Overlay" IsHitTestVisible="False">
                        <Rectangle x:Name="SelectionRect" Visibility="Collapsed" StrokeThickness="2" />
                    </Canvas>
                </Grid>
            </ScrollViewer>

            <!-- Right: info box -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Cords and size" Margin="8,8,8,4" FontWeight="Bold" />
                <TextBox x:Name="InfoBox" Grid.Row="1" Margin="8" FontFamily="Consolas" FontSize="14"
                         IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap" />
            </Grid>
        </Grid>
    </Grid>
</Window>